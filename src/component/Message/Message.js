import React from 'react';

const Message = () => {
    return (
        <div className="message bg-warning font-weight-bold text-center p-2">
            don't say black lives matter! always say that "Human Lives Matter"
        </div>
    );
};

export default Message;